import React from "react";

const Separator = () => {

  return (
    <div className={`flex justify-center `}>
      <div className={`md:w-[250rem] w-full sm:w-[35rem] shadow-lg overflow-hidden h-2 bg-gradient-to-b from-[#292929] to-[#413f3f]   rounded-sm backdrop-blur-sm`}></div>
    </div>
  );
};

export default Separator;
